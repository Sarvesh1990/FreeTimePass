/*
package monzo.namit;

public class URL {
    private final java.net.URL url;

    public URL(String url) {
        this.url = url;
    }

    public boolean equals(Object other) {
        if (!(other instanceof URL)) {
            return false;
        } else {
            URL otherURL = (URL) other;
            return otherURL.url.equals(this.url);
        }
    }

    public int hashCode() {
        return this.url.hashCode();
    }


}
*/
